---
phase: 04-homepage-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/constants.ts
  - src/lib/schemas.ts
  - src/app/globals.css
  - src/components/sections/HeroSection.tsx
  - src/components/sections/ServicesSection.tsx
  - package.json
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04, SRVC-01, SRVC-02, SRVC-03]

must_haves:
  truths:
    - "Hero section fills full viewport with dark animated gradient background and 60% black overlay"
    - "SATIN FABRICATION renders in Playfair 7xl-8xl with gold accent on SATIN, staggered entrance animation"
    - "VIEW OUR WORK CTA button has gold outline that fills gold on hover"
    - "Pulsing scroll indicator animates continuously at hero bottom"
    - "4 service cards display in 2x2 grid on desktop, single column on mobile"
    - "Each service card has gold border that brightens on hover, image scales 1.03x on hover"
    - "Service cards stagger in with scroll-triggered reveal animation"
  artifacts:
    - path: "src/lib/constants.ts"
      provides: "SERVICES, PORTFOLIO_PROJECTS, PROCESS_STEPS, SOCIAL_LINKS data arrays"
      contains: "SERVICES"
    - path: "src/lib/schemas.ts"
      provides: "Zod contact form schema and ContactFormData type"
      exports: ["contactSchema", "ContactFormData"]
    - path: "src/app/globals.css"
      provides: "heroGradient keyframes animation"
      contains: "heroGradient"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Full-viewport hero with gradient background, staggered text, CTA, scroll indicator"
      exports: ["HeroSection"]
    - path: "src/components/sections/ServicesSection.tsx"
      provides: "4 service cards in responsive grid with hover effects"
      exports: ["ServicesSection"]
  key_links:
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/lib/constants.ts"
      via: "import SERVICES"
      pattern: "import.*SERVICES.*from.*constants"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/app/globals.css"
      via: "heroGradient animation reference"
      pattern: "heroGradient"
    - from: "src/components/sections/ServicesSection.tsx"
      to: "src/components/ui/MotionWrapper.tsx"
      via: "scroll reveal wrapper"
      pattern: "import.*MotionWrapper"
---

<objective>
Install Phase 4 dependencies, create the shared data layer (all section content data, Zod schema, hero gradient CSS), and build the Hero and Services sections.

Purpose: Establishes the data foundation all 6 sections consume and delivers the first two visible sections -- the hero that replaces the placeholder page and the services grid that showcases the business offerings.

Output: HeroSection.tsx, ServicesSection.tsx, extended constants.ts with all section data, schemas.ts with Zod contact form schema, heroGradient keyframes in globals.css, and 4 new npm packages installed.
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage-sections/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/components/ui/MotionWrapper.tsx:
```typescript
interface MotionWrapperProps {
  children: ReactNode
  delay?: number
  className?: string
  as?: ElementType
}
export function MotionWrapper({ children, delay, className, as }: MotionWrapperProps)
```

From src/lib/constants.ts:
```typescript
export const NAV_LINKS = [
  { label: 'WORK', href: '#work' },
  { label: 'SERVICES', href: '#services' },
  { label: 'PROCESS', href: '#process' },
  { label: 'CONTACT', href: '#contact' },
] as const
export const SCROLL_THRESHOLD = 50
export const CURSOR_LERP_SPEED = 0.15
```

From src/app/globals.css (existing micro-interaction classes):
```css
.btn-glow { transition: box-shadow 0.3s var(--ease-luxury); }
.btn-glow:hover { box-shadow: 0 0 20px rgba(201, 169, 110, 0.3), 0 0 40px rgba(201, 169, 110, 0.1); }
.input-luxury { border-bottom: 1px solid rgba(201, 169, 110, 0.3); transition: border-color 0.3s var(--ease-luxury); }
.input-luxury:focus { border-color: var(--color-gold); outline: none; }
```

From src/app/layout.tsx:
```typescript
// Layout wraps children in: SkipLink > Providers(LazyMotion+MotionConfig) > Navbar > {children} > CustomCursor > GrainOverlay
// All animated components MUST use m.* (not motion.*) — LazyMotion strict mode
```

Design tokens available (from globals.css @theme):
```
--color-void: #0A0A0A    --color-charcoal: #1A1A1A   --color-surface: #111111
--color-gold: #C9A96E    --color-gold-light: #D4B87A  --color-gold-dim: #8A7544
--color-cream: #F5F0EB   --color-warm-gray: #8A8278   --ease-luxury: cubic-bezier(0.25, 0.1, 0.25, 1)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create data layer</name>
  <files>package.json, src/lib/constants.ts, src/lib/schemas.ts, src/app/globals.css</files>
  <action>
1. Install new dependencies: `npm install zod@"^3.24" react-hook-form @hookform/resolvers lucide-react`

2. Extend `src/lib/constants.ts` — append (do not modify existing NAV_LINKS, SCROLL_THRESHOLD, CURSOR_LERP_SPEED) these data arrays:

- `SERVICES` array with 4 objects: `{ title, description, image }` for Railings & Staircases, Gates & Fencing, Custom Furniture & Fixtures, Fireplace Surrounds & Features. Each image is a curated Unsplash URL with `?w=640&h=400&fit=crop&q=80` params. Use dark-toned metalwork images — search for photo IDs showing: metal staircase railing, ornamental iron gate, custom metal furniture/shelving, modern fireplace surround. Mark array `as const`.

- `PORTFOLIO_PROJECTS` array with 9 objects: `{ title, category, image, width, height }`. Use varied aspect ratios for masonry effect (mix of 600x800 portrait, 800x600 landscape, 600x600 square). Categories should reference the 4 service types. Curate Unsplash URLs with matching `?w=WIDTH&h=HEIGHT&fit=crop&q=80` params. All images must have dark, moody metalwork tone. Mark array `as const`.

- `PROCESS_STEPS` array with 5 objects: `{ number: '01'|'02'|'03'|'04'|'05', title, description }` for Consultation, Design, Material Selection, Fabrication, Installation. Each description is 1-2 sentences. Mark array `as const`.

- `SOCIAL_LINKS` array with 3 objects: `{ icon: 'Instagram'|'Facebook'|'Linkedin', href: '#', label: string }`. The `icon` field stores a string name (the actual lucide-react import happens in FooterSection). Mark array `as const`.

3. Create `src/lib/schemas.ts`:
```typescript
import { z } from 'zod'

export const contactSchema = z.object({
  name: z.string().min(1, 'Name is required'),
  email: z.string().email('Please enter a valid email address'),
  phone: z.string().min(1, 'Phone number is required'),
  projectType: z.enum(
    ['Railings & Staircases', 'Gates & Fencing', 'Custom Furniture & Fixtures', 'Fireplace Surrounds', 'Other'],
    { errorMap: () => ({ message: 'Please select a project type' }) }
  ),
  description: z.string().min(10, 'Please describe your project (at least 10 characters)'),
})

export type ContactFormData = z.infer<typeof contactSchema>
```

4. Add `heroGradient` keyframes to `src/app/globals.css` — insert AFTER the existing `@keyframes reveal` block and BEFORE the `@media (prefers-reduced-motion)` block:
```css
@keyframes heroGradient {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
```
  </action>
  <verify>
    <automated>cd "/Users/kaideninglis/Desktop/SatinFabrication website" && npm run build</automated>
  </verify>
  <done>All 4 new packages installed (zod, react-hook-form, @hookform/resolvers, lucide-react). constants.ts exports SERVICES (4 items), PORTFOLIO_PROJECTS (9 items), PROCESS_STEPS (5 items), SOCIAL_LINKS (3 items). schemas.ts exports contactSchema and ContactFormData. globals.css contains heroGradient keyframes. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build HeroSection and ServicesSection components</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/ServicesSection.tsx</files>
  <action>
Create `src/components/sections/` directory if it does not exist.

**HeroSection.tsx** (`'use client'`):
- Full-viewport section with `min-h-screen`, `flex items-center justify-center`, `overflow-hidden`, `relative`
- Animated gradient background: an absolutely-positioned div with `background: linear-gradient(135deg, #0A0A0A 0%, #1A1A1A 25%, #111111 50%, #0D0D0D 75%, #0A0A0A 100%)`, `backgroundSize: '400% 400%'`, `animation: 'heroGradient 20s ease infinite'`; set `aria-hidden="true"`
- Dark overlay: absolutely-positioned div with `bg-black/60`, `aria-hidden="true"`
- Content container: `relative z-10 text-center px-6`
- Staggered entrance using `m.h1`, `m.p`, `m.div` from `motion/react` (NOT `motion.*`):
  - `m.h1`: "SATIN FABRICATION" — `font-display text-5xl md:text-7xl lg:text-8xl font-bold text-cream tracking-tight`. Wrap "SATIN" in `<span className="text-gold">`. Initial `{ opacity: 0, y: 30 }`, animate `{ opacity: 1, y: 0 }`, transition `{ duration: 0.7, ease: [0.25, 0.1, 0.25, 1] }`
  - `m.p`: "CUSTOM ARCHITECTURAL METALWORK" — `font-body text-sm md:text-base tracking-[0.3em] uppercase text-warm-gray mt-4 md:mt-6`. Delay 0.15s
  - `m.div`: CTA button wrapper. Delay 0.3s. Contains an anchor `<a href="#work">` styled as button: `inline-block px-8 py-3 mt-8 border border-gold text-gold font-body text-sm tracking-widest uppercase transition-all duration-300 hover:bg-gold hover:text-void btn-glow`. Text: "VIEW OUR WORK"
- Pulsing scroll indicator at bottom: `m.div` with `absolute bottom-8 left-1/2 -translate-x-1/2`, animate `{ y: [0, 8, 0] }`, transition `{ duration: 2, repeat: Infinity, ease: 'easeInOut' }`, `aria-hidden="true"`. Contains a thin line: `<div className="h-8 w-[1px] bg-gold/50" />`
- No section id on hero (it's the page top)

**ServicesSection.tsx** (`'use client'`):
- `<section id="services" className="py-24 px-6">`
- Max-width container: `mx-auto max-w-6xl`
- Section heading wrapped in `MotionWrapper`: "OUR <span className="text-gold">SERVICES</span>" in `font-display text-4xl md:text-5xl text-cream text-center mb-16`
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-6`
- Map over `SERVICES` array, each card wrapped in `MotionWrapper` with `delay={i * 0.1}`:
  - Card div: `group bg-surface border border-gold-dim/30 rounded overflow-hidden transition-colors duration-300 hover:border-gold`
  - Image container: `overflow-hidden aspect-[16/10]`
  - `next/image` with `width={640} height={400}` and `className="w-full h-full object-cover transition-transform duration-500 ease-[cubic-bezier(0.25,0.1,0.25,1)] group-hover:scale-[1.03]"`
  - Card body: `p-6` with Playfair title (`font-display text-xl text-cream`), Raleway description (`mt-2 font-body text-warm-gray text-sm leading-relaxed`), gold "Learn More" link (`mt-4 inline-block font-body text-sm text-gold hover:text-gold-light transition-colors`, text "Learn More →" linking to `#contact`)
  </action>
  <verify>
    <automated>cd "/Users/kaideninglis/Desktop/SatinFabrication website" && npm run build</automated>
  </verify>
  <done>HeroSection renders full-viewport hero with animated gradient, staggered text entrance (SATIN in gold), gold CTA button with hover fill, and pulsing scroll indicator. ServicesSection renders 4 cards in 2x2 grid (desktop) / 1-col (mobile) with gold border brighten and image 1.03x scale on hover, staggered scroll reveal. Both components use m.* (not motion.*). Build passes with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- HeroSection.tsx exports HeroSection component with 'use client' directive
- ServicesSection.tsx exports ServicesSection component with 'use client' directive
- constants.ts exports SERVICES (4), PORTFOLIO_PROJECTS (9), PROCESS_STEPS (5), SOCIAL_LINKS (3)
- schemas.ts exports contactSchema and ContactFormData
- globals.css contains heroGradient keyframes
- All animated components use m.* from motion/react
</verification>

<success_criteria>
- Hero fills viewport with dark animated gradient and 60% overlay
- Text entrance animates with stagger (SATIN in gold, tagline, CTA)
- CTA button gold-outlined with gold fill on hover
- Scroll indicator pulses continuously
- 4 service cards in responsive 2x2 / 1-col grid
- Card hover: border brightens to full gold, image scales 1.03x
- All data arrays available for Plans 02 and 03
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage-sections/04-01-SUMMARY.md`
</output>
