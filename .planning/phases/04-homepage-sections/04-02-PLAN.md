---
phase: 04-homepage-sections
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/sections/PortfolioSection.tsx
  - src/components/sections/ProcessSection.tsx
autonomous: true
requirements: [PORT-01, PORT-02, PORT-04, PROC-01, PROC-03]

must_haves:
  truths:
    - "Portfolio section shows 8-10 metalwork images in a masonry grid with 3 columns desktop, 2 tablet, 1 mobile"
    - "Hovering a portfolio image fades in an overlay with project name and category in elegant typography"
    - "Portfolio images are curated Unsplash metalwork placeholders with consistent dark tone"
    - "Process section shows 5 numbered steps with gold connecting line that animates on scroll"
    - "Step numbers render in Playfair Display 6xl with low-opacity gold behind the step text"
  artifacts:
    - path: "src/components/sections/PortfolioSection.tsx"
      provides: "Masonry gallery with hover overlays using MasonryGrid/MasonryItem"
      exports: ["PortfolioSection"]
    - path: "src/components/sections/ProcessSection.tsx"
      provides: "5-step timeline section wrapping ProcessTimeline component"
      exports: ["ProcessSection"]
  key_links:
    - from: "src/components/sections/PortfolioSection.tsx"
      to: "src/components/ui/MasonryGrid.tsx"
      via: "import MasonryGrid, MasonryItem"
      pattern: "import.*MasonryGrid.*MasonryItem.*from"
    - from: "src/components/sections/PortfolioSection.tsx"
      to: "src/lib/constants.ts"
      via: "import PORTFOLIO_PROJECTS"
      pattern: "import.*PORTFOLIO_PROJECTS.*from.*constants"
    - from: "src/components/sections/ProcessSection.tsx"
      to: "src/components/ui/ProcessTimeline.tsx"
      via: "import ProcessTimeline"
      pattern: "import.*ProcessTimeline.*from"
    - from: "src/components/sections/ProcessSection.tsx"
      to: "src/lib/constants.ts"
      via: "import PROCESS_STEPS"
      pattern: "import.*PROCESS_STEPS.*from.*constants"
---

<objective>
Build the Portfolio gallery section and Process timeline section, composing Phase 3 UI primitives (MasonryGrid, ProcessTimeline) with content data from the data layer created in Plan 01.

Purpose: These two sections form the middle of the homepage scroll narrative -- showcasing completed work (portfolio) and explaining the engagement workflow (process). Both sections compose directly from reusable primitives built in Phase 3, making this primarily a composition and styling task.

Output: PortfolioSection.tsx (masonry gallery with hover overlays) and ProcessSection.tsx (5-step timeline with section heading).
</objective>

<execution_context>
@/Users/kaideninglis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kaideninglis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-homepage-sections/04-RESEARCH.md
@.planning/phases/04-homepage-sections/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/MasonryGrid.tsx:
```typescript
interface MasonryGridProps { children: ReactNode; className?: string }
export function MasonryGrid({ children, className }: MasonryGridProps)
// Renders: columns-1 sm:columns-2 lg:columns-3 gap-4

interface MasonryItemProps { children: ReactNode; className?: string }
export function MasonryItem({ children, className }: MasonryItemProps)
// Renders: break-inside-avoid mb-4
```

From src/components/ui/ProcessTimeline.tsx:
```typescript
interface TimelineStep { number: string; title: string; description: string }
interface ProcessTimelineProps { steps: TimelineStep[]; className?: string }
export function ProcessTimeline({ steps, className }: ProcessTimelineProps)
// Renders: SVG pathLength scroll-animated gold line, staggered step reveals
// Already implements: large step numbers in text-6xl text-gold/10, scroll reveal per step
```

From src/components/ui/MotionWrapper.tsx:
```typescript
export function MotionWrapper({ children, delay, className, as }: MotionWrapperProps)
// Scroll-triggered fade+translateY reveal with luxury easing
```

From src/lib/constants.ts (created in Plan 01):
```typescript
export const PORTFOLIO_PROJECTS: readonly { title: string; category: string; image: string; width: number; height: number }[]
// 9 items with varied aspect ratios, Unsplash metalwork URLs

export const PROCESS_STEPS: readonly { number: string; title: string; description: string }[]
// 5 items: Consultation, Design, Material Selection, Fabrication, Installation
```

Design tokens: void=#0A0A0A, charcoal=#1A1A1A, surface=#111111, gold=#C9A96E, gold-light=#D4B87A, gold-dim=#8A7544, cream=#F5F0EB, warm-gray=#8A8278
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PortfolioSection with masonry grid and hover overlays</name>
  <files>src/components/sections/PortfolioSection.tsx</files>
  <action>
Create `src/components/sections/PortfolioSection.tsx` (`'use client'`):

- `<section id="work" className="py-24 px-6">` — NOTE: id is "work" (not "portfolio") to match NAV_LINKS `#work`
- Max-width container: `mx-auto max-w-6xl`
- Section heading wrapped in `MotionWrapper`: "OUR <span className="text-gold">WORK</span>" in `font-display text-4xl md:text-5xl text-cream text-center mb-16`
- Import `MasonryGrid` and `MasonryItem` from `@/components/ui/MasonryGrid`
- Import `PORTFOLIO_PROJECTS` from `@/lib/constants`
- Import `Image` from `next/image`
- Map over `PORTFOLIO_PROJECTS`, each item wrapped in `MasonryItem`:
  - Outer div: `group relative overflow-hidden rounded`
  - `Image` component with `src={project.image}`, `alt={project.title}`, `width={project.width}`, `height={project.height}`, `className="w-full block"`
  - Hover overlay div: `absolute inset-0 flex flex-col justify-end p-4 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 ease-[cubic-bezier(0.25,0.1,0.25,1)]`
    - Project title: `font-display text-lg text-cream`
    - Project category: `font-body text-sm text-gold`
- Wrap the entire MasonryGrid in a `MotionWrapper` for scroll reveal on the section
  </action>
  <verify>
    <automated>cd "/Users/kaideninglis/Desktop/SatinFabrication website" && npm run build</automated>
  </verify>
  <done>PortfolioSection renders 9 metalwork images in masonry grid (3/2/1 columns responsive). Each image shows overlay on hover with project name and category. Section id is "work" matching nav link. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProcessSection wrapping ProcessTimeline with section heading</name>
  <files>src/components/sections/ProcessSection.tsx</files>
  <action>
Create `src/components/sections/ProcessSection.tsx` (`'use client'`):

- `<section id="process" className="py-24 px-6 bg-charcoal/50">` — subtle background differentiation
- Max-width container: `mx-auto max-w-4xl`
- Section heading wrapped in `MotionWrapper`: "OUR <span className="text-gold">PROCESS</span>" in `font-display text-4xl md:text-5xl text-cream text-center mb-16`
- Optional subheading in `MotionWrapper` with `delay={0.1}`: "From concept to completion, every project follows our proven five-step approach." in `font-body text-warm-gray text-center mb-16 max-w-2xl mx-auto`
- Import `ProcessTimeline` from `@/components/ui/ProcessTimeline`
- Import `PROCESS_STEPS` from `@/lib/constants`
- Render `<ProcessTimeline steps={PROCESS_STEPS} />` — the ProcessTimeline component already handles:
  - SVG pathLength scroll-animated gold connecting line
  - Staggered step reveals with whileInView
  - Large step numbers in `font-display text-6xl text-gold/10` (satisfies PROC-03)
  - Step titles in Playfair and descriptions in Raleway

NOTE: The ProcessTimeline component from Phase 3 already implements PROC-02 (animated gold connecting line) and PROC-03 (large step numbers in low-opacity gold). This section wrapper provides the section heading, id, and passes the 5-step data array (PROC-01).
  </action>
  <verify>
    <automated>cd "/Users/kaideninglis/Desktop/SatinFabrication website" && npm run build</automated>
  </verify>
  <done>ProcessSection renders section heading and 5-step timeline with scroll-animated gold connecting line. Step numbers display in 6xl low-opacity gold behind text. Section id is "process" matching nav link. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- PortfolioSection.tsx exports PortfolioSection component
- ProcessSection.tsx exports ProcessSection component
- Portfolio section id="work" (matches NAV_LINKS #work)
- Process section id="process" (matches NAV_LINKS #process)
- Both components use m.* from motion/react (via MotionWrapper and ProcessTimeline)
- Portfolio images use next/image with explicit width/height (no layout shift)
</verification>

<success_criteria>
- Portfolio shows 9 images in masonry 3/2/1 column layout
- Portfolio hover overlay fades in with project name and gold category text
- Process shows 5 steps with animated gold connecting line on scroll
- Step numbers appear in large low-opacity gold behind step text
- Both sections respond to scroll with reveal animations
</success_criteria>

<output>
After completion, create `.planning/phases/04-homepage-sections/04-02-SUMMARY.md`
</output>
